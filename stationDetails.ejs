<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title><%= station.name %> - Details</title>
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 mx-auto">
                <h1 class="mb-4"><%= station.name %></h1>
                <img src="/images/<%= station.image %>" width="40%"class="img-fluid mb-4">
                <p class="lead"><%= station.description %></p>
                <p><strong>Language:</strong> <%= station.language %></p>
                <p><strong>Stream URL:</strong> <a href="<%= station.streamUrl %>"><%= station.streamUrl %></a></p>
                
                <% if (loggedIn) { %>
                    <form action="/addStation/<%= station.id %>" method="POST">
                        <button type="submit" class="btn btn-primary">Add to Favourites</button>
                    </form>
                <% } else { %>
                    <p><a href="/login" class="btn btn-secondary">Login</a> to add to favourites</p>
                <% } %>
                
                <a href="/stations" class="btn btn-link">Back to Stations</a>
            </div>
        </div>
    </div>
</body>
</html>



